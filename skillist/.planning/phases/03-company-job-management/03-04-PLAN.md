---
phase: 03-company-job-management
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified: [src/app/jobs/page.tsx, src/app/jobs/[id]/page.tsx]
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "Users can browse all active job listings"
    - "Users can view detailed information for a specific job"
    - "Required skills for a job are visible to applicants"
  artifacts:
    - path: "src/app/jobs/page.tsx"
      provides: "Public job discovery list"
    - path: "src/app/jobs/[id]/page.tsx"
      provides: "Detailed job view with skill requirements"
  key_links:
    - from: "Job List Item"
      to: "/jobs/[id]"
      via: "Next.js Link"
    - from: "Job Detail Page"
      to: "job_skills join table"
      via: "Drizzle relational query"
---

<objective>
Develop the public job discovery experience, allowing candidates to browse active listings and view detailed requirements, facilitating the connection between talent and opportunity.

Purpose: To expose hiring needs to candidates.
Output: Server-rendered job listing and detail pages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-company-job-management/03-RESEARCH.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public Job Listing Page</name>
  <files>src/app/jobs/page.tsx</files>
  <action>
    Create a server component at src/app/jobs/page.tsx that:
    1. Fetches all jobs with status 'active' from the database.
    2. Includes the associated company name (via join).
    3. Renders a searchable/filterable list of job cards.
    4. Each card shows: Title, Company, Location, Salary, and a few top skill tags.
  </action>
  <verify>
    <automated># Manual verification by visiting /jobs</automated>
  </verify>
  <done>Public users can browse all open job opportunities.</done>
</task>

<task type="auto">
  <name>Task 2: SSR Job Detail Page</name>
  <files>src/app/jobs/[id]/page.tsx</files>
  <action>
    Create a server component at src/app/jobs/[id]/page.tsx that:
    1. Fetches the specific job by ID, including company details and all associated `job_skills`.
    2. Renders a comprehensive view:
       - Header: Title, Company, Date Posted, Status.
       - Body: Job description, Location, Salary Range.
       - Sidebar/Section: "Required Skills" with proficiency levels.
    3. Handles 404 cases gracefully if the job doesn't exist or isn't active.
    4. Includes an "Apply" button (placeholder for Phase 5, or linking to an external URL if provided).
  </action>
  <verify>
    <automated># Manual verification by visiting /jobs/[id]</automated>
  </verify>
  <done>Candidates can see the full requirements and context for a job posting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> Browser | Public data only (only 'active' jobs should be visible) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | Draft jobs | mitigate | Filter queries to only return `status = 'active'` for public pages |
| T-03-04-02 | Information Disclosure | Private company info | mitigate | Only select necessary public fields from the `companies` table |
</threat_model>

<verification>
1. Navigate to `/jobs`.
2. Verify all active jobs are listed.
3. Click on a job to view its details.
4. Verify all information, especially required skills, is displayed correctly.
</verification>

<success_criteria>
Public job discovery is functional with server-side rendering for SEO and performance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-company-job-management/03-04-SUMMARY.md`
</output>
