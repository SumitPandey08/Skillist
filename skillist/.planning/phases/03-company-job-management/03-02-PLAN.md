---
phase: 03-company-job-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/app/(dashboard)/dashboard/page.tsx, src/app/(dashboard)/dashboard/company/jobs/page.tsx, src/app/dashboard/_actions.ts]
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "Company users see a dedicated job management dashboard"
    - "Company users can view a list of their job postings"
    - "Company users can close or delete their jobs"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/company/jobs/page.tsx"
      provides: "Job listing interface for companies"
    - path: "src/app/dashboard/_actions.ts"
      provides: "updateJobStatus and deleteJob server actions"
  key_links:
    - from: "/dashboard"
      to: "/dashboard/company/jobs"
      via: "role-based redirect"
---

<objective>
Create the company dashboard view for listing and managing jobs, allowing employers to track their hiring needs and update job statuses.

Purpose: To provide employers with a central hub for job management.
Output: Role-based dashboard redirection and job listing page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-company-job-management/03-RESEARCH.md
@src/app/(dashboard)/dashboard/page.tsx
@src/app/dashboard/_actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role-Based Dashboard Redirection</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    Update the main dashboard page to redirect users based on their role:
    - If student: redirect to '/dashboard/profile' (existing).
    - If company: redirect to '/dashboard/company/jobs'.
    - Use Clerk `auth().userId` and query the `users` table to determine the role.
  </action>
  <verify>
    <automated># Manual verification via Clerk auth simulation or integration test</automated>
  </verify>
  <done>Users are correctly routed to their respective dashboard views.</done>
</task>

<task type="auto">
  <name>Task 2: Company Job Listing Page</name>
  <files>src/app/(dashboard)/dashboard/company/jobs/page.tsx</files>
  <action>
    Create a new page at src/app/(dashboard)/dashboard/company/jobs/page.tsx that:
    1. Fetches all jobs belonging to the authenticated company.
    2. Displays them in a list or table using shadcn/ui.
    3. Shows job status (Draft, Active, Closed) with visual indicators (Badge).
    4. Provides actions for each job: "Edit" (link to /edit), "Close" (button), "Delete" (button).
    5. Includes a "Post New Job" button linking to /jobs/new.
  </action>
  <verify>
    <automated># Component rendering test or manual visual check</automated>
  </verify>
  <done>Company users can view and manage their job postings.</done>
</task>

<task type="auto">
  <name>Task 3: Job Management Server Actions</name>
  <files>src/app/dashboard/_actions.ts</files>
  <action>
    Implement the following server actions:
    1. `updateJobStatus(jobId: string, status: JobStatus)`: Updates the status of a job.
    2. `deleteJob(jobId: string)`: Deletes a job.
    - IMPORTANT: Verify that the authenticated user owns the job before performing updates or deletions.
    - Use `revalidatePath('/dashboard/company/jobs')` after operations.
  </action>
  <verify>
    <automated>vitest tests/job-actions.test.ts (if created) or manual testing</automated>
  </verify>
  <done>Server actions correctly update and delete job data with authorization checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Server Actions | Authentication must be verified; Ownership must be checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | deleteJob action | mitigate | Verify `auth().userId` matches the job's `companyId` before deletion |
| T-03-02-02 | Tampering | updateJobStatus | mitigate | Verify ownership and status transition rules |
</threat_model>

<verification>
1. Log in as a Company user.
2. Navigate to `/dashboard`.
3. Verify redirection to `/dashboard/company/jobs`.
4. Verify list view renders correctly (even if empty).
</verification>

<success_criteria>
Company users have a functional job management interface with proper authorization.
</success_criteria>

<output>
After completion, create `.planning/phases/03-company-job-management/03-02-SUMMARY.md`
</output>
