---
phase: 03-company-job-management
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/app/(dashboard)/dashboard/company/jobs/new/page.tsx, src/app/(dashboard)/dashboard/company/jobs/[id]/edit/page.tsx, src/components/jobs/skill-tag-input.tsx, src/app/dashboard/_actions.ts, package.json]
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "Company can create jobs with multi-select skill tagging"
    - "Company can edit job details and required skills"
    - "Skill suggestions come from the global skills table"
  artifacts:
    - path: "src/components/jobs/skill-tag-input.tsx"
      provides: "Reusable skill input with autocomplete via emblor"
    - path: "src/app/(dashboard)/dashboard/company/jobs/new/page.tsx"
      provides: "Job creation form"
    - path: "src/app/(dashboard)/dashboard/company/jobs/[id]/edit/page.tsx"
      provides: "Job editing interface"
  key_links:
    - from: "Job Creation Form"
      to: "createJob server action"
      via: "React Hook Form + Zod"
    - from: "Job Editing Form"
      to: "updateJob server action"
      via: "React Hook Form + Zod"
---

<objective>
Implement the job creation and editing workflows, featuring a robust skill-tagging interface that allows employers to explicitly define the required skills for their roles.

Purpose: To capture detailed job requirements for the matching engine.
Output: Integrated job creation and editing forms with `emblor` skill tagging.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-company-job-management/03-RESEARCH.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies and Scaffold Component</name>
  <files>package.json, src/components/jobs/skill-tag-input.tsx</files>
  <action>
    1. Install `emblor` and `date-fns` as specified in RESEARCH.md.
    2. Create a reusable `SkillTagInput` component using `emblor`.
    - Component should accept an array of selected skills.
    - Component should fetch skill suggestions from the global `skills` table (via a server action or client-side fetch).
    - Component should integrate with `react-hook-form` via a controlled input.
  </action>
  <verify>
    <automated>npm list emblor date-fns</automated>
  </verify>
  <done>Dependencies installed and `SkillTagInput` component available for use.</done>
</task>

<task type="auto">
  <name>Task 2: Job Creation Flow</name>
  <files>src/app/(dashboard)/dashboard/company/jobs/new/page.tsx, src/app/dashboard/_actions.ts</files>
  <action>
    1. Implement the `createJob` server action in src/app/dashboard/_actions.ts.
       - Use `zod` to validate the job input (title, description, location, salary, type, skills).
       - Ensure the `companyId` is correctly linked.
       - Create the `jobs` entry first, then populate the `job_skills` join table.
       - Normalize skill names before inserting.
    2. Create the job creation page at src/app/(dashboard)/dashboard/company/jobs/new/page.tsx.
       - Use shadcn/ui Form, Input, Textarea, and the `SkillTagInput`.
       - On success, redirect to `/dashboard/company/jobs`.
  </action>
  <verify>
    <automated># Manual verification by creating a new job and checking DB</automated>
  </verify>
  <done>Employers can successfully post new job listings with skill tags.</done>
</task>

<task type="auto">
  <name>Task 3: Job Editing Flow</name>
  <files>src/app/(dashboard)/dashboard/company/jobs/[id]/edit/page.tsx, src/app/dashboard/_actions.ts</files>
  <action>
    1. Implement the `updateJob` server action.
       - IMPORTANT: Verify user ownership.
       - Update the `jobs` table and handle synchronization of the `job_skills` join table (diffing and updating).
    2. Create the job editing page at src/app/(dashboard)/dashboard/company/jobs/[id]/edit/page.tsx.
       - Prefill the form with existing job data.
       - Allow modifying all fields including skills.
  </action>
  <verify>
    <automated># Manual verification by editing an existing job</automated>
  </verify>
  <done>Employers can successfully update existing job details and requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> createJob / updateJob | All inputs must be validated by Zod schema; Ownership must be verified for updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | updateJob server action | mitigate | Verify `auth().userId` owns the job before applying updates |
| T-03-03-02 | Input Injection | description field | mitigate | Sanitize or strictly validate the description via Zod and Next.js default protections |
</threat_model>

<verification>
1. Navigate to `/dashboard/company/jobs/new`.
2. Fill out the form, add skills, and submit.
3. Verify the job appears in the list and database.
4. Click "Edit" for the created job, modify fields/skills, and save.
5. Verify changes persist.
</verification>

<success_criteria>
Job creation and editing are fully functional with integrated skill tagging.
</success_criteria>

<output>
After completion, create `.planning/phases/03-company-job-management/03-03-SUMMARY.md`
</output>
