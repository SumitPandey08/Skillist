---
phase: 03-company-job-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/__tests__/schema.test.ts]
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "Jobs can be stored in the DB with status (draft/active/closed)"
    - "Jobs are linked to companies"
    - "Jobs can have multiple required skills via a join table"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Job and job_skills table definitions"
      contains: "export const jobs"
    - path: "src/__tests__/schema.test.ts"
      provides: "Validation for the new tables"
  key_links:
    - from: "jobs.companyId"
      to: "companies.id"
      via: "foreign key"
    - from: "jobSkills.jobId"
      to: "jobs.id"
      via: "foreign key"
    - from: "jobSkills.skillId"
      to: "skills.id"
      via: "foreign key"
---

<objective>
Extend the database schema to support job postings and their associated skill requirements, forming the foundation for company job management and future AI matching.

Purpose: To provide a structured data model for employer hiring needs.
Output: Updated schema with `jobs` and `job_skills` tables.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-company-job-management/03-RESEARCH.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Job Schema in Drizzle</name>
  <files>src/db/schema.ts</files>
  <action>
    Add the following to src/db/schema.ts (referencing 03-RESEARCH.md):
    1. Define `jobStatusEnum`: `['draft', 'active', 'closed']`.
    2. Define `jobs` table with:
       - `id`: text primary key (nanoid)
       - `companyId`: text references companies.id (cascade delete)
       - `title`: text not null
       - `description`: text not null
       - `location`: text
       - `salaryRange`: text
       - `jobType`: text (e.g., 'full-time', 'part-time')
       - `status`: jobStatusEnum (default 'draft')
       - `createdAt`, `updatedAt`: timestamps
    3. Define `jobSkills` join table with:
       - `jobId`: text references jobs.id (cascade delete)
       - `skillId`: text references skills.id (cascade delete)
       - `requiredProficiency`: text (default 'beginner')
       - Primary key: [jobId, skillId]
  </action>
  <verify>
    <automated>npm run db:push -- --force</automated>
  </verify>
  <done>Schema updated and pushed to database without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Schema Tests</name>
  <files>src/__tests__/schema.test.ts</files>
  <behavior>
    - Should be able to insert a job linked to a company.
    - Should be able to associate skills with a job in the job_skills table.
    - Should enforce foreign key constraints for jobs and job_skills.
  </behavior>
  <action>
    Update src/__tests__/schema.test.ts to include tests for the new `jobs` and `job_skills` tables. Ensure they correctly handle relationships and status enums.
  </action>
  <verify>
    <automated>npm test src/__tests__/schema.test.ts</automated>
  </verify>
  <done>Schema tests pass for the new tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> Schema | Database must correctly enforce constraints defined in schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | jobs table | mitigate | Use foreign key constraints with cascade deletes to maintain integrity |
| T-03-02 | Information Disclosure | jobs table | mitigate | Ensure status enum is used to distinguish between public and draft jobs in queries (later plans) |
</threat_model>

<verification>
1. Run `npm run db:push` to apply changes.
2. Run `npm test` to verify schema integrity.
</verification>

<success_criteria>
`jobs` and `job_skills` tables exist in the database with the correct columns and relationships.
</success_criteria>

<output>
After completion, create `.planning/phases/03-company-job-management/03-01-SUMMARY.md`
</output>
