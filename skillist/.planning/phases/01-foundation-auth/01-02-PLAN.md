---
phase: "01-foundation-auth"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01-01"]
files_modified:
  - "src/app/layout.tsx"
  - "src/middleware.ts"
  - "src/app/(auth)/sign-in/[[...sign-in]]/page.tsx"
  - "src/app/(auth)/sign-up/[[...sign-up]]/page.tsx"
autonomous: true
requirements: ["AUTH-01", "AUTH-02"]

must_haves:
  truths:
    - "User can access sign-in and sign-up pages"
    - "Middleware correctly identifies public and protected routes"
    - "Unauthenticated users are redirected to sign-in for protected routes"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Route protection and onboarding redirection"
      contains: "clerkMiddleware"
    - path: "src/app/layout.tsx"
      provides: "ClerkProvider integration"
      contains: "ClerkProvider"
  key_links:
    - from: "src/middleware.ts"
    - to: "Clerk Auth"
      via: "clerkMiddleware"
---

<objective>
Integrate Clerk authentication into the ECHFLUX platform, enabling secure user signup/signin and protecting private routes using role-based middleware logic.

Purpose: Ensure only authorized users can access the application's core features.
Output: Functional authentication flow with protected routes and initial onboarding redirection.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Clerk Provider and Environment</name>
  <files>src/app/layout.tsx, .env.local</files>
  <action>
    Wrap the root layout with `<ClerkProvider>`.
    Configure environment variables for Clerk (Publishable Key and Secret Key) in `.env.local`.
    Define `NEXT_PUBLIC_CLERK_SIGN_IN_URL`, `NEXT_PUBLIC_CLERK_SIGN_UP_URL`, `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL`, and `NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL` in `.env.local`.
  </action>
  <verify>
    <automated>grep "ClerkProvider" src/app/layout.tsx</automated>
  </verify>
  <done>Clerk is integrated into the application layout and environment variables are set.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Clerk Middleware with Onboarding Check</name>
  <files>src/middleware.ts</files>
  <action>
    Implement `clerkMiddleware` to protect all routes except public ones (`/`, `/sign-in`, `/sign-up`, `/api/webhooks(.*)`).
    Add logic to check if a user has completed onboarding (using `sessionClaims.metadata.onboardingComplete`).
    Redirect authenticated users who haven't completed onboarding to `/onboarding` (unless they are already on that route).
    Refer to Pattern 1 in `01-RESEARCH.md`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/middleware.test.ts</automated>
  </verify>
  <done>Route protection and onboarding redirection logic is active.</done>
</task>

<task type="auto">
  <name>Task 3: Create Authentication Pages</name>
  <files>src/app/(auth)/sign-in/[[...sign-in]]/page.tsx, src/app/(auth)/sign-up/[[...sign-up]]/page.tsx</files>
  <action>
    Create custom sign-in and sign-up pages using Clerk's `<SignIn />` and `<SignUp />` components.
    Place them under the `(auth)` route group for clean organization.
    Configure the components to use the standard auth layout.
  </action>
  <verify>
    <automated>npx playwright test tests/auth.spec.ts</automated>
  </verify>
  <done>User can visit the sign-in and sign-up pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Clerk | User credentials sent directly to Clerk. |
| Browser -> App | Clerk JWT session token used for authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | Auth Session | mitigate | Clerk handles session security and token validation. |
| T-01-04 | Elevation of Privilege | Middleware | mitigate | Middleware checks metadata for onboarding status; subsequent phases will check specific roles. |
</threat_model>

<verification>
Start dev server and attempt to access a protected route (e.g., `/dashboard`).
Verify redirect to `/sign-in`.
Sign up as a new user and verify redirect to `/onboarding`.
</verification>

<success_criteria>
- Sign-in and Sign-up pages are functional.
- Unauthenticated users cannot access protected routes.
- Authenticated users without onboarding data are redirected to `/onboarding`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
