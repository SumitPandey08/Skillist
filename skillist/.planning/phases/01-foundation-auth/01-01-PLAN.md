---
phase: "01-foundation-auth"
plan: "01"
type: "execute"
wave: 1
depends_on: ["01-00"]
files_modified:
  - "package.json"
  - "src/db/schema.ts"
  - "src/db/index.ts"
  - "drizzle.config.ts"
  - ".env.local"
autonomous: true
requirements: ["AUTH-01", "AUTH-02"]
user_setup:
  - service: "Supabase"
    why: "Primary database"
    env_vars:
      - name: "DATABASE_URL"
        source: "Supabase Project Settings -> Database -> Connection string (Transaction mode)"
  - service: "Clerk"
    why: "Authentication"
    env_vars:
      - name: "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY"
        source: "Clerk Dashboard -> API Keys"
      - name: "CLERK_SECRET_KEY"
        source: "Clerk Dashboard -> API Keys"

must_haves:
  truths:
    - "Database connection established via Drizzle ORM"
    - "Core schema tables (users, students, companies) exist in Supabase"
    - "Project scaffolded with Next.js 16 App Router"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle table definitions"
      contains: "users, students, companies"
    - path: "src/db/index.ts"
      provides: "Drizzle database client"
    - path: "drizzle.config.ts"
      provides: "Drizzle migration configuration"
  key_links:
    - from: "src/db/index.ts"
      to: "process.env.DATABASE_URL"
      via: "Drizzle instantiation"
      pattern: "drizzle\\("
---

<objective>
Initialize the core ECHFLUX project infrastructure, establish the database connection with Drizzle ORM and Supabase, and define the foundational user schema for a "Skills-First" model.

Purpose: Provide the stable foundation for authentication and user data management.
Output: Scaffolded Next.js 16 project with a connected PostgreSQL database and defined schema.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-00-SUMMARY.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and core dependencies</name>
  <files>package.json, next.config.ts, src/app/page.tsx</files>
  <action>
    Initialize a new Next.js 16 project (using App Router, TypeScript, and Tailwind).
    Install core dependencies: `@clerk/nextjs`, `drizzle-orm`, `pg`, `svix`, `zod`, `lucide-react`, `clsx`, `tailwind-merge`.
    Install dev dependencies: `drizzle-kit`, `@types/pg`.
    Set up basic folder structure: `src/app`, `src/components`, `src/lib`, `src/db`.
  </action>
  <verify>
    <automated>npm run dev -- --port 3001 & sleep 5 && curl -I http://localhost:3001</automated>
  </verify>
  <done>Next.js project is running, folder structure is established.</done>
</task>

<task type="auto">
  <name>Task 2: Define Drizzle schema and DB client</name>
  <files>src/db/schema.ts, src/db/index.ts, drizzle.config.ts, .env.local</files>
  <action>
    Create `src/db/schema.ts` defining `users`, `students`, and `companies` tables as per the Architectural Research.
    Include `pgvector` support in `students` table for future AI matching (1536 dimensions for OpenAI embeddings).
    Implement `src/db/index.ts` to export the Drizzle DB client using `pg` driver.
    Create `drizzle.config.ts` for schema management.
    Add `DATABASE_URL` placeholder to `.env.local`.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Drizzle schema is valid and database client is correctly configured.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to Supabase</name>
  <files>.env.local</files>
  <action>
    Ensure `DATABASE_URL` is correctly set in `.env.local` (User setup required).
    Run `npx drizzle-kit push` to synchronize the schema with the Supabase database.
    Note: If `pgvector` extension is not enabled in Supabase, the user might need to enable it manually in the Supabase SQL editor: `CREATE EXTENSION IF NOT EXISTS vector;`.
  </action>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <done>Database schema is successfully synchronized with Supabase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Database | Drizzle client connects to Supabase via DATABASE_URL; credentials must be protected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local | mitigate | Use .gitignore to prevent committing secrets; use Clerk for authentication. |
| T-01-02 | Tampering | Database | mitigate | Enforce Row Level Security (RLS) in Supabase (deferred to Phase 1, Wave 3). |
</threat_model>

<verification>
Check `src/db/schema.ts` for table definitions and `drizzle.config.ts` for connection configuration.
Run `npx drizzle-kit check` to ensure schema validity.
Verify database connection by running a simple query (e.g., in a test).
</verification>

<success_criteria>
- Next.js 16 project initialized and running.
- Drizzle ORM configured with Supabase connection.
- `users`, `students`, and `companies` tables created in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
