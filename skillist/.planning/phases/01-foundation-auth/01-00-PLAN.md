---
phase: "01-foundation-auth"
plan: "00"
type: "execute"
wave: 0
depends_on: []
files_modified:
  - "vitest.config.ts"
  - "playwright.config.ts"
  - "tests/auth.spec.ts"
  - "tests/onboarding.spec.ts"
  - "src/__tests__/webhook.test.ts"
  - "src/__tests__/middleware.test.ts"
  - "src/__tests__/schema.test.ts"
autonomous: true
requirements: ["AUTH-01", "AUTH-02"]

must_haves:
  truths:
    - "Test infrastructure is initialized"
    - "Scaffolded test files exist for subsequent plans"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration"
    - path: "playwright.config.ts"
      provides: "Playwright configuration"
    - path: "tests/auth.spec.ts"
      provides: "E2E Auth test scaffold"
    - path: "tests/onboarding.spec.ts"
      provides: "E2E Onboarding test scaffold"
    - path: "src/__tests__/webhook.test.ts"
      provides: "Integration Webhook test scaffold"
    - path: "src/__tests__/middleware.test.ts"
      provides: "Unit Middleware test scaffold"
    - path: "src/__tests__/schema.test.ts"
      provides: "Drizzle Schema test scaffold"
---

<objective>
Initialize the testing infrastructure and scaffold the necessary test files to support the Nyquist verification requirements for Phase 01.

Purpose: Enable automated verification for all subsequent development tasks.
Output: Configured testing environment and placeholder test files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize testing configuration</name>
  <files>vitest.config.ts, playwright.config.ts</files>
  <action>
    Create `vitest.config.ts` with basic React/TypeScript support.
    Create `playwright.config.ts` configured for Next.js (base URL http://localhost:3000).
    Install necessary dev dependencies: `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@playwright/test`.
  </action>
  <verify>
    <automated>ls vitest.config.ts playwright.config.ts</automated>
  </verify>
  <done>Testing configurations are present.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold placeholder test files</name>
  <files>tests/auth.spec.ts, tests/onboarding.spec.ts, src/__tests__/webhook.test.ts, src/__tests__/middleware.test.ts, src/__tests__/schema.test.ts</files>
  <action>
    Create empty test scaffolds with "TODO" or "Skip" status to ensure they can be referenced by later plans without failing the build.
    - `tests/auth.spec.ts`: Playwright E2E for Clerk flows.
    - `tests/onboarding.spec.ts`: Playwright E2E for role selection.
    - `src/__tests__/webhook.test.ts`: Vitest for Clerk sync logic.
    - `src/__tests__/middleware.test.ts`: Vitest for Next.js middleware.
    - `src/__tests__/schema.test.ts`: Vitest for Drizzle schema verification.
  </action>
  <verify>
    <automated>ls tests/auth.spec.ts tests/onboarding.spec.ts src/__tests__/webhook.test.ts src/__tests__/middleware.test.ts src/__tests__/schema.test.ts</automated>
  </verify>
  <done>All required test files are scaffolded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (Testing infrastructure)

## STRIDE Threat Register
N/A
</threat_model>

<verification>
Verify that all files exist and `npm run test` (or equivalent) can find them, even if they contain no active tests yet.
</verification>

<success_criteria>
- `vitest.config.ts` and `playwright.config.ts` are created.
- Four placeholder test files are created in their respective directories.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-00-SUMMARY.md`
</output>
