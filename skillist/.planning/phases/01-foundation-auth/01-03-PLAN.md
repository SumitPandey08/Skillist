---
phase: "01-foundation-auth"
plan: "03"
type: "execute"
wave: 3
depends_on: ["01-02"]
files_modified:
  - "src/app/api/webhooks/clerk/route.ts"
  - "src/app/onboarding/page.tsx"
  - "src/app/(dashboard)/profile/page.tsx"
  - "src/app/(dashboard)/settings/page.tsx"
  - "src/components/onboarding-form.tsx"
autonomous: false
requirements: ["AUTH-01", "AUTH-02"]
user_setup:
  - service: "Clerk"
    why: "Webhook sync"
    dashboard_config:
      - task: "Configure Webhook"
        location: "Clerk Dashboard -> Webhooks -> Add Endpoint (URL: {deployment_url}/api/webhooks/clerk)"

must_haves:
  truths:
    - "User can choose their role (Student or Company) during onboarding"
    - "Profile data (Name, Primary Skill/Company Name) is saved to the database"
    - "Clerk webhooks successfully sync user data to the database"
    - "Users can view and edit their profile and settings"
  artifacts:
    - path: "src/app/api/webhooks/clerk/route.ts"
      provides: "Clerk sync handler"
      contains: "svix"
    - path: "src/app/onboarding/page.tsx"
      provides: "Onboarding UI"
      contains: "Student, Company"
    - path: "src/components/onboarding-form.tsx"
      provides: "Role-specific onboarding steps"
  key_links:
    - from: "Clerk Dashboard"
      to: "src/app/api/webhooks/clerk/route.ts"
      via: "Webhook"
    - from: "Onboarding Flow"
      to: "Drizzle DB"
      via: "Server Actions"
---

<objective>
Complete the initial user journey by implementing a multi-step onboarding flow for role selection, syncing data between Clerk and the local database via webhooks, and providing basic profile and settings management.

Purpose: Collect essential user data and establish the role-based experience for Students and Companies.
Output: Functional onboarding flow, data sync, and profile management pages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Clerk Webhook Handler</name>
  <files>src/app/api/webhooks/clerk/route.ts, .env.local</files>
  <action>
    Create a POST route at `/api/webhooks/clerk` to handle Clerk events (`user.created`, `user.updated`).
    Use `svix` for webhook signature verification.
    Implement logic to insert or update user records in the `users` table based on Clerk data.
    Refer to Pattern 2 in `01-RESEARCH.md`.
    Define `CLERK_WEBHOOK_SECRET` in `.env.local`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/webhook.test.ts</automated>
  </verify>
  <done>Database user records are correctly synced with Clerk events.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Clerk Webhook Handler</what-built>
  <how-to-verify>
    1. Start the local server with an ngrok or cloudflared tunnel to expose the webhook endpoint.
    2. Configure the Clerk Webhook in the Clerk Dashboard.
    3. Manually create a user in Clerk (e.g., via the Clerk Dashboard).
    4. Check the local database (`users` table) to see if the record was created.
  </how-to-verify>
  <resume-signal>User record created in database</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build Multi-Step Onboarding Flow</name>
  <files>src/app/onboarding/page.tsx, src/components/onboarding-form.tsx</files>
  <action>
    Create the `/onboarding` page and a `OnboardingForm` component.
    Implement role selection (Student vs. Company) using shadcn/ui Radio Groups or Cards.
    Collect role-specific data:
      - Student: Name, Primary Skill (tag selection).
      - Company: Name, Company Name, Industry.
    Use Server Actions to:
      1. Save the data to `students` or `companies` tables.
      2. Update the `users` table with the selected role (use an **upsert** pattern to avoid race conditions with the Clerk webhook).
      3. Update the user's `publicMetadata` in Clerk (setting `onboardingComplete: true` and `role`).
    Redirect user to `/dashboard` upon completion.
  </action>
  <verify>
    <automated>npx playwright test tests/onboarding.spec.ts</automated>
  </verify>
  <done>User can complete onboarding and their role/profile is persisted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Onboarding Flow</what-built>
  <how-to-verify>
    1. Sign up as a new user.
    2. Complete the onboarding flow.
    3. Verify that the user is redirected to the dashboard.
    4. Check the database to confirm the profile data (name, role, etc.) was saved correctly.
    5. Verify in Clerk Dashboard that `publicMetadata` was updated.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create Profile and Settings Pages</name>
  <files>src/app/(dashboard)/profile/page.tsx, src/app/(dashboard)/settings/page.tsx</files>
  <action>
    Implement basic `/profile` and `/settings` pages.
    `/profile`: Display user information (name, role, etc.) and provide an edit form.
    `/settings`: Display Clerk's `<UserProfile />` component for account management.
    Use `auth()` and database queries to fetch current user data.
  </action>
  <verify>
    <automated>curl -I http://localhost:3001/profile && curl -I http://localhost:3001/settings</automated>
  </verify>
  <done>User can view and manage their profile and account settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook -> App | Unauthenticated Clerk webhook event arrives from external source. |
| Browser -> Server | User submits onboarding form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Webhook Payload | mitigate | Use `svix` for signature verification to ensure payloads come from Clerk. |
| T-01-06 | Information Disclosure | Onboarding Form | mitigate | Use Zod for server-side validation and sanitization. |
| T-01-07 | Information Disclosure | Profile Page | mitigate | Ensure only logged-in users can access their own profile. |
</threat_model>

<verification>
Test the entire user journey: Sign-up -> Onboarding -> Profile -> Logout -> Sign-in.
Verify database integrity at each step.
</verification>

<success_criteria>
- Multi-step onboarding flow is fully functional and persisted.
- Clerk webhooks keep the database in sync.
- Profile and Settings pages are accessible and show correct data.
- Metadata in Clerk reflects the user's onboarding status.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
