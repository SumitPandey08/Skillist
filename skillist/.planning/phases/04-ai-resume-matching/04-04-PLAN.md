---
phase: 04-ai-resume-matching
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified: [src/lib/ai/generator.ts, src/components/resume/pdf-template.tsx, src/app/(dashboard)/student/resume/tailor/page.tsx, src/app/(dashboard)/student/resume/_actions.ts]
autonomous: true
requirements: [STU-03]

must_haves:
  truths:
    - "Students can generate a resume tailored to a specific job description."
    - "AI-generated resume content is optimized for ATS (keyword integration, professional tone)."
    - "The system produces a valid, downloadable PDF file."
    - "The resume layout is professional and consistent with modern standards."
  artifacts:
    - path: "src/lib/ai/generator.ts"
      provides: "AI tailoring logic"
    - path: "src/components/resume/pdf-template.tsx"
      provides: "Declarative PDF template"
    - path: "src/app/(dashboard)/student/resume/tailor/page.tsx"
      provides: "Resume tailoring UI"
  key_links:
    - from: "src/lib/ai/generator.ts"
      to: "OpenAI gpt-4o-mini"
      via: "Generative prompt for tailoring"
    - from: "src/components/resume/pdf-template.tsx"
      to: "@react-pdf/renderer"
      via: "PDF rendering engine"
---

<objective>
Empower students to present their best selves to employers by generating job-specific, ATS-optimized resumes. This plan leverages AI to rewrite and prioritize profile content based on the target job's requirements and renders the result as a high-quality PDF.

Purpose: Increase student interview rates through targeted application materials.
Output: Tailoring logic, PDF templates, and resume generation UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-resume-matching/04-RESEARCH.md
@.planning/phases/04-ai-resume-matching/04-01-SUMMARY.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AI Resume Tailoring Logic</name>
  <files>src/lib/ai/generator.ts</files>
  <behavior>
    - Given a student profile (full DB data) and a job description:
      - Rewrite professional summary to highlight relevant experience.
      - Rephrase experience bullets to emphasize skills required by the JD.
      - Select and prioritize projects that align with job duties.
    - Result: A structured JSON object representing the tailored resume.
  </behavior>
  <action>
    - Implement `generateTailoredResume(studentId, jobId)` in `src/lib/ai/generator.ts`.
    - Use `gpt-4o-mini` with a structured output schema that matches the PDF template's needs.
    - Ensure prompts focus on "ATS optimization" (keywords, active voice).
  </action>
  <verify>
    <automated>npm test src/__tests__/ai/generator.test.ts</automated>
  </verify>
  <done>AI tailoring engine is capable of generating JD-optimized resume data.</done>
</task>

<task type="auto">
  <name>Task 2: Create ATS-Optimized PDF Template</name>
  <files>src/components/resume/pdf-template.tsx</files>
  <action>
    - Use `@react-pdf/renderer` to build a professional, single-column resume template.
    - Sections: Header (contact info), Summary, Skills, Experience, Projects, Education.
    - Ensure clean typography and proper spacing (standard ATS-friendly design).
    - Implement a helper to render the component to a PDF blob or stream.
  </action>
  <verify>
    <automated>npm test src/__tests__/ui/pdf-template.test.ts</automated>
  </verify>
  <done>Professional PDF resume template is ready for production.</done>
</task>

<task type="auto">
  <name>Task 3: Build Resume Tailoring & Download UI</name>
  <files>src/app/(dashboard)/student/resume/tailor/page.tsx, src/app/(dashboard)/student/resume/_actions.ts</files>
  <action>
    - Create a page at `/dashboard/student/resume/tailor` where students can:
      - Select a job (either from their applied jobs or a search/list).
      - Trigger the tailoring process.
      - Preview the tailored content (using `react-markdown` if desired).
      - Download the resulting PDF.
    - Implement `getTailoredResumeAction(jobId)` Server Action to orchestrate the generation and return the data.
    - Add a "Tailor Resume" button to Job Detail pages for easy access.
  </action>
  <verify>
    <automated>npm test src/__tests__/ui/tailor-page.test.ts</automated>
  </verify>
  <done>Students can generate and download job-specific resumes directly from the platform.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | PDF binary data and tailored content are sent to the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | `generator.ts` | mitigate | Ensure tailoring logic only has access to the requesting student's data. |
| T-04-04-02 | Resource Exhaustion | PDF Generation | mitigate | Limit the number of resume generations per user per day to prevent API abuse. |
</threat_model>

<verification>
Verify that:
1. Selecting a job generates content that contains JD-specific keywords.
2. The downloaded PDF opens correctly and follows the professional template.
3. All profile sections (skills, exp, etc.) are correctly represented.
</verification>

<success_criteria>
1. Students can generate a PDF tailored to any active job.
2. The tailoring process takes less than 30 seconds.
3. The resulting PDF is valid and matches the design specs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-resume-matching/04-04-SUMMARY.md`
</output>
