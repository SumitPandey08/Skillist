---
phase: "02-student-portfolio"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["src/db/schema.ts", "package.json", "src/__tests__/schema.test.ts"]
autonomous: true
requirements: ["STU-01"]
must_haves:
  truths:
    - "New tables (skills, student_skills, projects, certifications) exist in database"
    - "Students table has slug and bio fields"
    - "Database push command succeeds"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Normalized schema for student portfolio"
      contains: "skills, student_skills, projects, certifications"
    - path: "package.json"
      provides: "New dependencies for slug generation"
      contains: "slugify, nanoid"
  key_links:
    - from: "src/db/schema.ts"
      to: "Postgres"
      via: "drizzle-kit push"
      pattern: "pgTable\\(\"skills\""
---

<objective>
Update the database schema to support student professional identity, including skills, projects, certifications, and public-facing slugs.

Purpose: Provide the persistent storage layer for the student portfolio hub (STU-01).
Output: Updated schema and verified database tables.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-portfolio/02-RESEARCH.md
@src/db/schema.ts

<interfaces>
From src/db/schema.ts:
```typescript
export const users = pgTable("users", {
  id: text("id").primaryKey(), // Clerk User ID
  email: text("email").notNull().unique(),
  role: userRoleEnum("role"),
})

export const students = pgTable("students", {
  id: text("id").primaryKey().references(() => users.id, { onDelete: 'cascade' }),
  name: text("name").notNull(),
  primarySkill: text("primary_skill"),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slugify and nanoid dependencies</name>
  <files>package.json</files>
  <action>
    Install slugify and nanoid to support unique, SEO-friendly portfolio URLs.
    Command: `npm install slugify@^1.6.6 nanoid@^5.1.0`
  </action>
  <verify>
    <automated>grep -E "\"slugify\":|\"nanoid\":" package.json</automated>
  </verify>
  <done>Dependencies added to package.json</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update database schema</name>
  <files>src/db/schema.ts</files>
  <behavior>
    - Ensure 'skills' table exists with id (uuid), name (text, unique), category (text).
    - Ensure 'student_skills' junction table exists with proficiency (integer).
    - Ensure 'projects' table exists with title, description, url, imageUrl, startDate, endDate.
    - Ensure 'certifications' table exists with name, issuer, issueDate, credentialUrl.
    - Add 'slug' (text, unique) and 'bio' (text) to 'students' table.
  </behavior>
  <action>
    Implement the schema updates as per 02-RESEARCH.md. Use drizzle-orm/pg-core components.
    Ensure all foreign keys reference correctly (referencing 'students.id').
  </action>
  <verify>
    <automated>npm run db:push -- --dry-run</automated>
  </verify>
  <done>Schema file updated and push dry-run passes</done>
</task>

<task type="auto">
  <name>Task 3: Apply schema and update tests</name>
  <files>src/__tests__/schema.test.ts</files>
  <action>
    1. Apply the schema changes to the database using `npm run db:push`.
    2. Update the existing schema verification tests in `src/__tests__/schema.test.ts` to include the new tables and fields.
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/schema.test.ts</automated>
  </verify>
  <done>Database schema applied and tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | User inputs for projects/skills cross this boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/db/schema.ts | mitigate | Use strict Zod validation in later dashboard plans for these fields |
| T-02-02 | Information Disclosure | src/db/schema.ts | mitigate | Ensure slug field is indexed and unique for safe public lookup |
</threat_model>

<verification>
Run `npm run db:push` followed by `npm run test` to ensure schema integrity.
</verification>

<success_criteria>
- Schema updated with 4 new tables.
- Students table has slug and bio fields.
- Tests pass for schema verification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-portfolio/02-01-SUMMARY.md`
</output>
