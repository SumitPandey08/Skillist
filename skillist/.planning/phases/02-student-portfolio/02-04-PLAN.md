---
phase: "02-student-portfolio"
plan: "04"
type: "execute"
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: ["src/lib/progress.ts", "src/components/dashboard/profile-completeness.tsx", "src/app/(dashboard)/dashboard/page.tsx", "src/components/dashboard/share-portfolio.tsx"]
autonomous: true
requirements: ["STU-01"]
must_haves:
  truths:
    - "Profile completeness score reflects the current state of the profile"
    - "Dashboard shows a progress bar or indicator"
    - "User can copy their public portfolio URL from the dashboard"
  artifacts:
    - path: "src/lib/progress.ts"
      provides: "Profile completeness calculation logic"
      contains: "calculateProfileCompleteness"
    - path: "src/components/dashboard/profile-completeness.tsx"
      provides: "UI widget for profile progress"
      contains: "Progress, Badge"
  key_links:
    - from: "src/components/dashboard/profile-completeness.tsx"
      to: "src/lib/progress.ts"
      via: "Function call"
      pattern: "calculateProfileCompleteness\\("
---

<objective>
Implement profile completion tracking and progress indicators to gamify and encourage students to complete their professional profiles.

Purpose: Increase profile quality and provide actionable feedback for students (STU-01).
Output: Profile completeness logic, dashboard widget, and sharing functionality.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-portfolio/02-RESEARCH.md
@src/app/(dashboard)/dashboard/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Progress Logic</name>
  <files>src/lib/progress.ts</files>
  <behavior>
    - 'calculateProfileCompleteness':
      - Bio: 20% if not null/empty.
      - Skills: 30% if at least 3 skills added.
      - Projects: 30% if at least 1 project added.
      - Certifications: 20% if at least 1 cert added.
      - Returns 0-100.
  </behavior>
  <action>
    Create a library for progress math. Use the recommended weightings from 02-RESEARCH.md.
  </action>
  <verify>
    <automated>ls src/lib/progress.ts</automated>
  </verify>
  <done>Progress logic implemented with tests (TDD)</done>
</task>

<task type="auto">
  <name>Task 2: Build Progress Dashboard Widget</name>
  <files>src/components/dashboard/profile-completeness.tsx, src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    - Create 'profile-completeness.tsx' using shadcn/ui `Progress` and `Badge` components.
    - Render the widget at the top of the student dashboard.
    - Display messages based on what's missing (e.g., "Add 3 skills to reach 50%").
  </action>
  <verify>
    <automated>grep "Progress" src/components/dashboard/profile-completeness.tsx</automated>
  </verify>
  <done>Progress indicators added to the student dashboard</done>
</task>

<task type="auto">
  <name>Task 3: Implement Share Portfolio Component</name>
  <files>src/components/dashboard/share-portfolio.tsx, src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    - Create a 'share-portfolio.tsx' component that displays the public URL and a 'Copy to Clipboard' button.
    - Render this component on the dashboard once the student has a slug.
    - Provide a quick link to 'View Public Portfolio'.
  </action>
  <verify>
    <automated>grep "Copy" src/components/dashboard/share-portfolio.tsx</automated>
  </verify>
  <done>Public URL sharing functionality implemented in the dashboard</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Server | Reading student's own data to calculate progress |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | src/components/dashboard/share-portfolio.tsx | accept | User's own public URL is meant to be shared |
</threat_model>

<verification>
Manual verification: Open dashboard, check progress score, add a skill, check if score increases. Copy the share URL and verify it works in a new tab.
</verification>

<success_criteria>
- Student dashboard has a visual progress indicator.
- Student can easily share their public portfolio URL.
- Logic correctly weights different profile sections.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-portfolio/02-04-SUMMARY.md`
</output>
