---
phase: "02-student-portfolio"
plan: "02"
type: "execute"
wave: 2
depends_on: ["02-01"]
files_modified: ["src/app/(dashboard)/dashboard/page.tsx", "src/app/dashboard/_actions.ts", "src/components/dashboard/skills-section.tsx", "src/components/dashboard/projects-section.tsx", "src/components/dashboard/certs-section.tsx"]
autonomous: true
requirements: ["STU-01"]
must_haves:
  truths:
    - "User can add/remove skills on the dashboard"
    - "User can add/edit/delete projects and certifications"
    - "Data is persisted and reflects in the UI after refresh"
  artifacts:
    - path: "src/app/dashboard/_actions.ts"
      provides: "CRUD actions for portfolio items"
      contains: "addSkill, addProject, addCertification"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard with profile management"
      contains: "Tabs, skills-section, projects-section"
  key_links:
    - from: "src/components/dashboard/skills-section.tsx"
      to: "src/app/dashboard/_actions.ts"
      via: "Server Action invocation"
      pattern: "addSkill\\("
---

<objective>
Implement the student dashboard UI for managing professional identity data (skills, projects, certifications).

Purpose: Enable students to maintain their portfolio content (STU-01).
Output: Functional dashboard sections for Skills, Projects, and Certifications.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-portfolio/02-RESEARCH.md
@src/db/schema.ts
@src/app/(dashboard)/dashboard/page.tsx

<interfaces>
From src/db/schema.ts (updated in 02-01):
```typescript
export const skills = pgTable("skills", { ... });
export const studentSkills = pgTable("student_skills", { ... });
export const projects = pgTable("projects", { ... });
export const certifications = pgTable("certifications", { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Server Actions for Portfolio CRUD</name>
  <files>src/app/dashboard/_actions.ts</files>
  <behavior>
    - 'addSkill': Associates a skill with a student, handles proficiency (0-100).
    - 'removeSkill': Removes association.
    - 'addProject': Creates new project record for student.
    - 'updateProject': Updates existing record.
    - 'deleteProject': Deletes record.
    - 'addCertification': Creates new cert record.
    - 'deleteCertification': Deletes record.
    - IMPORTANT: Every action must check `auth().userId` matches the `studentId`.
  </behavior>
  <action>
    Implement Server Actions using drizzle-orm. Use 'zod' for input validation.
  </action>
  <verify>
    <automated>ls src/app/dashboard/_actions.ts</automated>
  </verify>
  <done>Server actions file created with exportable CRUD functions</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dashboard UI Sections</name>
  <files>src/app/(dashboard)/dashboard/page.tsx, src/components/dashboard/skills-section.tsx, src/components/dashboard/projects-section.tsx, src/components/dashboard/certs-section.tsx</files>
  <action>
    - Add a `Tabs` interface to the dashboard for "Overview", "Skills", "Projects", "Certifications".
    - Create 'skills-section.tsx' with an 'Add Skill' combobox (using shadcn/ui).
    - Create 'projects-section.tsx' and 'certs-section.tsx' with Card-based lists and 'Add/Edit' Dialogs.
    - Integrate the Server Actions into the form 'onSubmit' handlers.
  </action>
  <verify>
    <automated>grep -r "Tabs" src/app/(dashboard)/dashboard/page.tsx</automated>
  </verify>
  <done>Dashboard UI sections implemented and wired to Server Actions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Server Actions | Untrusted input from user-controlled dashboard |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | src/app/dashboard/_actions.ts | mitigate | Check `auth().userId` against owner of project/skill in every action |
| T-02-04 | Tampering | src/app/dashboard/_actions.ts | mitigate | Use Zod schemas to validate types and constraints (e.g., proficiency 0-100) |
</threat_model>

<verification>
Manual verification: Open dashboard, add a skill, add a project, refresh to see if they persist.
</verification>

<success_criteria>
- Student can manage skills, projects, and certifications from the dashboard.
- Actions are protected by authentication and ownership checks.
- UI uses shadcn/ui components for a consistent experience.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-portfolio/02-02-SUMMARY.md`
</output>
