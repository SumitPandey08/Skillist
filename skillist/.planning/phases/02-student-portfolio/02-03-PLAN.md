---
phase: "02-student-portfolio"
plan: "03"
type: "execute"
wave: 2
depends_on: ["02-01"]
files_modified: ["src/lib/slugs.ts", "src/app/portfolio/[slug]/page.tsx", "src/app/portfolio/[slug]/layout.tsx", "src/__tests__/slugs.test.ts"]
autonomous: true
requirements: ["STU-02"]
must_haves:
  truths:
    - "Public portfolio is accessible at /portfolio/{slug}"
    - "Portfolio displays student bio, skills, projects, and certs"
    - "Slug is unique and generated correctly for new students"
  artifacts:
    - path: "src/lib/slugs.ts"
      provides: "Slug generation and lookup logic"
      contains: "generateStudentSlug, getStudentBySlug"
    - path: "src/app/portfolio/[slug]/page.tsx"
      provides: "Public portfolio page (SSR)"
      contains: "generateMetadata, ProfileContent"
  key_links:
    - from: "src/app/portfolio/[slug]/page.tsx"
      to: "Postgres"
      via: "Drizzle Query"
      pattern: "db.query.students.findFirst"
---

<objective>
Implement the public-facing portfolio hub with SEO-friendly slugs and SSR for optimal performance and indexing.

Purpose: Provide students with a shareable professional presence (STU-02).
Output: A public-facing SSR page displaying student portfolio data.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-portfolio/02-RESEARCH.md
@src/db/schema.ts

<interfaces>
From src/db/schema.ts (updated in 02-01):
```typescript
export const students = pgTable("students", {
  id: text("id").primaryKey(),
  slug: text("slug").unique(),
  ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Slug Logic</name>
  <files>src/lib/slugs.ts, src/__tests__/slugs.test.ts</files>
  <behavior>
    - 'generateStudentSlug': slugify(name) + '-' + nanoid(4).
    - 'getStudentBySlug': Fetches student by slug, including skills and projects.
    - Ensure uniqueness in generation.
  </behavior>
  <action>
    Create a library for slug management. Implement tests to verify uniqueness and correct generation patterns.
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/slugs.test.ts</automated>
  </verify>
  <done>Slug logic implemented and tested</done>
</task>

<task type="auto">
  <name>Task 2: Build Public Portfolio Hub</name>
  <files>src/app/portfolio/[slug]/page.tsx, src/app/portfolio/[slug]/layout.tsx</files>
  <action>
    - Create the page at `src/app/portfolio/[slug]/page.tsx` using Server Components.
    - Implement `generateMetadata` for SEO (Dynamic title, description, OG tags).
    - Design a clean, high-performance portfolio layout showcasing skills, projects (with links/images), and certifications.
    - Handle 'Not Found' state for invalid slugs.
  </action>
  <verify>
    <automated>grep "generateMetadata" src/app/portfolio/[slug]/page.tsx</automated>
  </verify>
  <done>Public portfolio page implemented with SEO support</done>
</task>

<task type="auto">
  <name>Task 3: Ensure Slugs for Existing Students</name>
  <files>src/app/onboarding/_actions.ts</files>
  <action>
    Update the `completeOnboarding` action to generate and save a slug when a student profile is created. 
    Also add a small script or update dashboard logic to retroactively generate slugs for existing students if they are null.
  </action>
  <verify>
    <automated>grep "generateStudentSlug" src/app/onboarding/_actions.ts</automated>
  </verify>
  <done>Slug generation integrated into the onboarding flow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public Internet → Portfolio Hub | Anonymous access to public profile data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | src/app/portfolio/[slug]/page.tsx | mitigate | Explicitly whitelist fields for the public view (don't send internal IDs or PII if not needed) |
| T-02-06 | Denial of Service | src/app/portfolio/[slug]/page.tsx | mitigate | Use Next.js caching (revalidate) to protect database from excessive public traffic |
</threat_model>

<verification>
Manual verification: Open `/portfolio/some-existing-slug` in a browser and check if it displays the data and correct SEO meta tags.
</verification>

<success_criteria>
- Portfolio hub is public and SEO-optimized.
- Slugs are human-readable and unique.
- Page performs well due to SSR/Caching.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-portfolio/02-03-SUMMARY.md`
</output>
