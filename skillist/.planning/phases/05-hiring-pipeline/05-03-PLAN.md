---
phase: 05-hiring-pipeline
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified: [src/app/dashboard/_actions.ts, src/components/jobs/status-select.tsx, src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/[appId]/page.tsx]
autonomous: true
requirements: [COMP-02]

must_haves:
  truths:
    - "Company can change the status of an application (e.g., Interviewing, Offered, Rejected)"
    - "Status changes are persisted in the database"
    - "UI provides immediate feedback on the status update"
  artifacts:
    - path: "src/components/jobs/status-select.tsx"
      provides: "Status update UI component"
  key_links:
    - from: "src/app/dashboard/_actions.ts"
      to: "db.update(applications)"
      via: "Server Action"
---

<objective>
Enable employers to manage the hiring pipeline by updating application statuses (Interviewing, Offered, Rejected, etc.).

Purpose: To allow employers to track progress with specific candidates.
Output: A functional status-update mechanism in the applicant detail view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hiring-pipeline/05-RESEARCH.md
@src/db/schema.ts
@src/app/dashboard/_actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create updateApplicationStatus server action</name>
  <files>src/app/dashboard/_actions.ts</files>
  <action>
    - Add the `updateApplicationStatus(appId, status)` server action to `src/app/dashboard/_actions.ts`.
    - Validate that the requesting user owns the job associated with the application.
    - Update the `status` and `updatedAt` fields in the `applications` table.
    - Revalidate the applicants list and detail pages.
  </action>
  <verify>
    <automated>grep -q "updateApplicationStatus" src/app/dashboard/_actions.ts</automated>
  </verify>
  <done>Server action for status updates is ready and secured.</done>
</task>

<task type="auto">
  <name>Task 2: Implement StatusSelect component</name>
  <files>src/components/jobs/status-select.tsx</files>
  <action>
    - Create a client-side `StatusSelect` component using Shadcn Select.
    - Options: Pending, Reviewed, Interviewing, Offered, Rejected.
    - Trigger the `updateApplicationStatus` action on change.
    - Add loading state and toast notification for feedback.
  </action>
  <verify>
    <automated>test -f src/components/jobs/status-select.tsx</automated>
  </verify>
  <done>UI component for status selection created with feedback loops.</done>
</task>

<task type="auto">
  <name>Task 3: Add status management to applicant detail view</name>
  <files>src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/[appId]/page.tsx</files>
  <action>
    - Integrate the `StatusSelect` component into the `[appId]/page.tsx` header or a dedicated actions section.
    - Ensure the current status is correctly reflected in the select initial value.
  </action>
  <verify>
    <automated>grep -q "StatusSelect" src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/[appId]/page.tsx</automated>
  </verify>
  <done>Applicant detail view now supports live status management.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Action -> Database | Ensure ownership check before updating application status. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Elevation of Privilege | `updateApplicationStatus` | mitigate | Explicitly check that the application's job belongs to the current user (company) before performing DB update. |
</threat_model>

<verification>
- Open an applicant detail view.
- Change the status from "Pending" to "Interviewing".
- Refresh the page and confirm the status change persists.
- Verify a toast notification appears.
</verification>

<success_criteria>
Employers can effectively move candidates through their internal hiring pipeline by updating their application status.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hiring-pipeline/05-03-SUMMARY.md`
</output>
