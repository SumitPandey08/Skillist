---
phase: 05-hiring-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/page.tsx, src/components/jobs/applicant-table.tsx, src/components/jobs/job-actions.tsx]
autonomous: true
requirements: [COMP-02, MATCH-03]

must_haves:
  truths:
    - "Company can see a list of applicants for a specific job"
    - "Applicants are automatically ranked by their Match Score by default"
    - "Company can navigate to this list from their job management dashboard"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/page.tsx"
      provides: "Ranked applicant list view"
    - path: "src/components/jobs/applicant-table.tsx"
      provides: "DataTable for candidates"
  key_links:
    - from: "src/components/jobs/job-actions.tsx"
      to: "/dashboard/company/jobs/[id]/applicants"
      via: "Link component"
---

<objective>
Implement the ranked applicant list for employers, providing a sortable data table that defaults to the highest match scores first.

Purpose: To allow employers to quickly identify the best-fitting candidates for a role.
Output: A new applicant list page with a functional DataTable.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hiring-pipeline/05-RESEARCH.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and update navigation</name>
  <files>package.json, src/components/jobs/job-actions.tsx</files>
  <action>
    - Install `@tanstack/react-table` for the applicant pipeline view.
    - Update `JobActions` component in `src/components/jobs/job-actions.tsx` to include a "View Applicants" button linking to `/dashboard/company/jobs/${jobId}/applicants`.
    - Use `Users` icon from Lucide for the new button.
  </action>
  <verify>
    <automated>npm list @tanstack/react-table && grep -q "View Applicants" src/components/jobs/job-actions.tsx</automated>
  </verify>
  <done>Dependencies installed and navigation link added to job cards.</done>
</task>

<task type="auto">
  <name>Task 2: Create Applicant DataTable component</name>
  <files>src/components/jobs/applicant-table.tsx</files>
  <action>
    - Create a reusable `ApplicantTable` component using `@tanstack/react-table`.
    - Columns: Student Name, Match Score, Applied Date, Status.
    - Implement default sorting by `matchScore` descending.
    - Add an "AI Pick" badge for the top 3 candidates (Match Score > 80).
    - Rows should be clickable, navigating to the applicant detail view.
  </action>
  <verify>
    <automated>test -f src/components/jobs/applicant-table.tsx</automated>
  </verify>
  <done>DataTable component created with sorting and ranking logic.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Applicants list page</name>
  <files>src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/page.tsx</files>
  <action>
    - Create the server component page at `src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/page.tsx`.
    - Fetch job details and applications for the given `id`.
    - Join `applications` with `students` to get candidate names and profile info.
    - Pass the data to the `ApplicantTable` component.
    - Ensure only the job owner can view the applicants (auth check).
  </action>
  <verify>
    <automated>test -f src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/page.tsx</automated>
  </verify>
  <done>Applicant list page functional and fetching real application data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Employer -> Application Data | Employers must only see applications for their own jobs. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | `applicants/page.tsx` | mitigate | Explicitly check that the `job.companyId` matches the current user's Clerk ID before rendering data. |
</threat_model>

<verification>
- Check if "View Applicants" appears on job cards.
- Verify clicking it leads to the ranked list.
- Confirm list is sorted by Match Score DESC.
</verification>

<success_criteria>
Employers can see a ranked list of all candidates who applied to their specific job posting.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hiring-pipeline/05-01-SUMMARY.md`
</output>
