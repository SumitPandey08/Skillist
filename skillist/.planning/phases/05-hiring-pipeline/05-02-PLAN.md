---
phase: 05-hiring-pipeline
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [package.json, src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/[appId]/page.tsx, src/components/jobs/score-radar-chart.tsx, src/components/ui/chart.tsx]
autonomous: true
requirements: [COMP-02]

must_haves:
  truths:
    - "Company can view detailed score breakdown for each applicant"
    - "Score visualization clearly shows Skill, Experience, Projects, and Potential categories"
    - "The AI reasoning/analysis text is visible to the employer"
  artifacts:
    - path: "src/components/jobs/score-radar-chart.tsx"
      provides: "Visual Radar chart component"
    - path: "src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/[appId]/page.tsx"
      provides: "Applicant detail drill-down view"
  key_links:
    - from: "src/components/jobs/applicant-table.tsx"
      to: "/dashboard/company/jobs/[id]/applicants/[appId]"
      via: "Row click navigation"
---

<objective>
Implement the applicant detail view with a Radar chart visualization, providing employers with a visual breakdown of candidate fit categories.

Purpose: To provide recruiters with a deeper understanding of why a candidate was ranked highly.
Output: An interactive applicant detail page with a score Radar chart.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hiring-pipeline/05-RESEARCH.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and Shadcn Chart</name>
  <files>package.json, src/components/ui/chart.tsx</files>
  <action>
    - Install `recharts` for visualization.
    - Initialize the `Chart` component from Shadcn if not already present.
    - Reference: `npx shadcn@latest add chart`
  </action>
  <verify>
    <automated>npm list recharts && test -f src/components/ui/chart.tsx</automated>
  </verify>
  <done>Visualization dependencies and UI components installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Score Radar Chart component</name>
  <files>src/components/jobs/score-radar-chart.tsx</files>
  <action>
    - Create a `ScoreRadarChart` using Recharts.
    - Data points: Skills, Experience, Projects, Potential (mapped from `skillScore`, `expScore`, `projScore`, `potentialScore`).
    - Use the Radar chart pattern with proper tooltips and styling.
  </action>
  <verify>
    <automated>test -f src/components/jobs/score-radar-chart.tsx</automated>
  </verify>
  <done>Radar chart component developed and ready for display.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Applicant detail page</name>
  <files>src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/[appId]/page.tsx</files>
  <action>
    - Create the detail page that fetches full application data, including student profile and AI analysis.
    - Render the `ScoreRadarChart` and display the `analysis` text.
    - Include student bio and contact info.
    - Auth check: Ensure the requesting user owns the job associated with this application.
  </action>
  <verify>
    <automated>test -f src/app/(dashboard)/dashboard/company/jobs/[id]/applicants/[appId]/page.tsx</automated>
  </verify>
  <done>Detailed applicant view is functional and shows visualized scores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Employer -> Personal Student Info | Contact info should only be accessible once they apply. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | `[appId]/page.tsx` | mitigate | Ensure the `appId` belongs to a `jobId` that the current user owns. |
</threat_model>

<verification>
- Navigate from the list view to a specific applicant.
- Verify the Radar chart displays the correct data points.
- Confirm AI analysis text is present.
</verification>

<success_criteria>
Employers can drill down into any applicant and see their categorical fit through a visual chart and AI reasoning.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hiring-pipeline/05-02-SUMMARY.md`
</output>
